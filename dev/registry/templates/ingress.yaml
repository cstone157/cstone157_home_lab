{{/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Chart.Name }}-ing
  namespace: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
    date: {{ now | htmlDate }}
    version: {{ .Chart.Version | quote }}
spec:
  rules:
  - host: registry.tmdash2.lab.shocn.lan
    http:
      paths:
      - backend:
          service:
            name: {{ .Chart.Name }}
            port:
              number: 5000
        path: /
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - registry.tmdash2.lab.shocn.lan
    secretName: {{ .Chart.Name }}-tls-ingress
*/}}
{{- if .Values.jupyter }}
{{- $object := .Values.jupyter -}}
{{- if not $object.version -}}
    {{- $_ := set $object "version" .Values.global.version -}}
{{- end }}
{{- if not $object.namespace -}}
    {{- $_ := set $object "namespace" .Values.global.namespace -}}
{{- end }}

{{- if $object.enabled }}
{{/* Add Service if they are required */}} 
{{- template "mychart.service" $object }}

{{/* Add Statefulset if they are required */}}
{{- template "mychart.statefulset" $object }}

{{/* Add PersistentVolumeClaims if they are required */}}
{{- template "mychart.persistent_volume_claim" $object }}

{{/* Add PersistentVolumes if they are required */}}
{{- template "mychart.persistent_volume" $object }}
{{- end }}

{{/*
apiVersion: v1
kind: PersistentVolume
metadata:
#   name: jupyter-local-pv
# spec:
#   capacity:
#     storage: 10Gi
#   accessModes:
#     - ReadWriteOnce
#   persistentVolumeReclaimPolicy: Retain
#   storageClassName: local-storage 
#   local:
#     path: /home/cstone/jupyter-data
  name: {{ $object.name }}-local-pv
spec:
  capacity:
    storage: {{ $object.volume.size }}
  accessModes:
    - {{ .Values.Lab.persistent_volume.access_modes }}
  persistentVolumeReclaimPolicy: {{ .Values.Lab.persistent_volume.persistent_volume_reclaim_policy }}
  storageClassName: {{ .Values.Lab.persistent_volume.storage_class_name }} 
  local:
    path: {{ .Values.Lab.persistent_volume.volume_path }}/{{ $object.volume.folder_name }}
  # nodeAffinity:
  #   required:
  #     nodeSelectorTerms:
  #       - matchExpressions:
  #           - key: kubernetes.io/hostname
  #             operator: In
  #             values:
  #               - lima-rancher-desktop
  # nodeSelector:
  #   matchLabels:
  #     kubernetes.io/hostname: lima-rancher-desktop 
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
#   name: jupyter-pvc
# spec:
#   accessModes:
#     - ReadWriteOnce
#   resources:
#     requests:
#       storage: 10Gi 
#   storageClassName: local-storage
  name: {{ $object.name }}-pvc
spec:
  accessModes:
    - {{ .Values.Lab.persistent_volume.access_modes }}
  resources:
    requests:
      storage: {{ $object.volume.size }} 
  storageClassName: {{ .Values.Lab.persistent_volume.storage_class_name }} 
---
kind: Service
apiVersion: v1
metadata:
  name: {{ $object.name }}
spec:
  type: NodePort
  selector:
    app: {{ $object.name }}
  ports:
  - protocol: {{ $object.HttpPort.protocol }}
    nodePort: {{ $object.HttpPort.nodePort }}
    port: {{ $object.HttpPort.port }}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ $object.name }}
  labels:
    app: {{ $object.label }}
spec:
  replicas: {{ .replicaCount }}
  serviceName: {{ $object.name }}
  selector:
    matchLabels:
      app: {{ $object.selector_name }}
  template:
    metadata:
      labels:
        app: {{ $object.selector_name }}
    spec:
      containers:
        - name: {{ $object.name }}
          ports:
            - containerPort: {{ $object.HttpPort.port  }}
              name: {{ $object.HttpPort.name  }}
              protocol: {{ $object.HttpPort.protocol }}
          env:
          - name: JUPYTER_PORT  
            value: {{ quote $object.HttpPort.port }}
          - name: RESTARTABLE
            value: "yes"
          image: {{ $object.image }}
          imagePullPolicy: "IfNotPresent"
          volumeMounts: 
          - mountPath: {{ $object.volume.mount_path }} 
            name: {{ $object.name }}-pvc
      volumes:
      - name: {{ $object.name }}-pvc
        persistentVolumeClaim:
          claimName: {{ $object.name }}-pvc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jupyter-notebook
  namespace: stone-data-lake
  labels:
    app: jupyter-notebook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jupyter-notebook
  template:
    metadata:
      labels:
        app: jupyter-notebook
    spec:
      containers:
      - name: minimal-notebook
        #image: jupyter/minimal-notebook:latest
        image: localhost:5000/data-lake-jupyter
        ports:
        - containerPort: 8888
        command: ["start-notebook.sh"]
        args: ["--NotebookApp.token=''"]
        volumeMounts:
        - mountPath: "/home/jovyan/work"
          name: local-storage
      volumes:
      - name: local-storage
        persistentVolumeClaim:
          claimName: example-local-pvc
#        volumeMounts: 
#        - mountPath: /home/jovyan/work
#          name: jupyter-work-volume
#          #readOnly: false
#      volumes: 
#      - name: jupyter-work-volume
#        hostPath:
#          path: work
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: example-local-pv-01
  namespace: stone-data-lake
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
#    path-for-windows: /run/desktop/mnt/host/c/PATH/TO/FILE
#    path: /run/desktop/mnt/host/c/Users/c.stone/Documents/GitHub/data_lake_example/jupyter/work
    path: ${jupyter_local_path}
#    path: ${_local_volume_path}
#    path: /c/Users/c.stone/Documents/GitHub/data_lake_example/jupyter/work
#    path: /mnt/disks/ssd1
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - docker-desktop
#                - node-01
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-local-pvc
  namespace: stone-data-lake
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
# Weirdly the storage for PVC must be less than the persistent-volumes size    
      storage: 5Gi
*/}}

{{- end }}
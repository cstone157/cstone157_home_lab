{{- $object := .Values -}}
{{- if $object.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: stone-data-lake
  labels:
    app: keycloak
spec:
  ports:
  - name: https
    port: 30000
    targetPort: 8080
  selector:
    app: keycloak
  type: LoadBalancer
{{- end }}
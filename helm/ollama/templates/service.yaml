apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-ollama
spec:
  selector:
    app: ollama
  ports:
  - protocol: {{ .Values.Lab.Ollama.HttpPort.protocol }}
    port: {{ .Values.Lab.Ollama.HttpPort.port }}
    targetPort: {{ .Values.Lab.Ollama.HttpPort.targetPort }}

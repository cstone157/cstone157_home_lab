apiVersion: v1
kind: Pod
metadata:
  name: jupyter-minimal
  labels:
    app: jupyter-minimal
spec:
  containers:
  - name: jupyter-minimal
    image: jupyter/minimal-notebook:latest  # or a specific version
    ports:
    - containerPort: 8888
      name: jupyter